venv/
node_modules/
.DS_Store
__pycache__/
*.pyc

# Secrets and credentials — never commit these
.env
*.key
*.pem
*.p12
*.pfx
keys.json
hub_secret.json
secrets.json

# CS spoke runtime state (JSON store, logs)
data/
cs.log
*.log

# SimQuotaEngine runtime ledger (written to data_dir; leaks to repo root when unset)
sim_quota_ledger.json
**/sim_quota_ledger.json

# LM agent worktrees (never track)
.claude/worktrees/

# mTLS material and runtime secrets issued by the hub — never commit these
certs/
.env.lock
*.crt
