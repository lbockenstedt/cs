fastapi>=0.111.0
uvicorn[standard]>=0.29.0
httpx>=0.27.0
websockets>=12.0
psutil>=5.9.0
python-dotenv>=1.0.0
# mDNS hub auto-discovery (_lm-hub._tcp.local.); the cs spoke subclasses
# BaseControlPlane which imports lm core's messaging.hub_discovery. Degrades to
# DNS-only if missing, but installing it lets the spoke hear the hub's broadcast.
zeroconf>=0.131,<1.0

# Phase 1 — simulation primitives + models
dnspython>=2.4          # dns_fail primitive (pure-Python DNS queries)
icmplib>=1.3            # ping_test primitive (optional; falls back to `ping`)
pydantic>=2             # data_models (already transitive via fastapi; pinned explicitly)

# Phase 3 — Proxmox client deployment (not imported until Phase 3)
# proxmoxer>=2.0